run:
  deadline: 10m

output:
  formats:
    text:
      path: stdout
      colors: true

linters:
  exclusions:
    paths:
      - deployments/infra/cdk.out
      - vendor

linters:
  settings:
    errcheck:
      disable-all: true